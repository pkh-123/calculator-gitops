apiVersion: v1
kind: Service

metadata:
  name: calculator
  namespace: calculator

spec:
  type: NodePort

  selector:
    app: calculator

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080